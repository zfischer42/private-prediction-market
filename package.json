{
  "name": "private-prediction-market",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "pnpm --filter @ppm/web dev",
    "build": "pnpm --filter @ppm/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "demo": "npm --prefix sandbox run demo"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.109.0"
  }
}
